<?php

namespace Config;

use CodeIgniter\Config\ForeignCharacters as BaseForeignCharacters;

/**
 * --------------------------------------------------------------------------
 * Foreign Characters Configuration
 * --------------------------------------------------------------------------
 * This class extends the base configuration for handling foreign characters.
 * It allows you to customize how foreign characters are processed or handled
 * within your application.
 * 
 * The class is marked as immutable to indicate that its configuration should
 * not be changed after initialization.
 */
class ForeignCharacters extends BaseForeignCharacters
{
    // No additional properties or methods are defined here; this class inherits
    // all functionality from BaseForeignCharacters and can be extended as needed.
}
